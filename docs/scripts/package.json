{
  "name": "cosmo-edge-docs-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Docs image optimization scripts (PNG/JPG → WebP) for cosmo-edge.",
  "type": "module",
  "scripts": {
    "convert:dry": "node convert-images.mjs",
    "convert:apply": "node convert-images.mjs --apply --delete",
    "refs:dry": "node rewrite-image-refs.mjs",
    "refs:apply": "node rewrite-image-refs.mjs --apply"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
