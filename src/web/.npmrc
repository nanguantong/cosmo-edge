# Locked to npmmirror for install speed.
# IMPORTANT: `npm audit` must be run with `--registry=https://registry.npmjs.org`
# because npmmirror does not implement /-/npm/v1/security/*.
registry=https://registry.npmmirror.com/
