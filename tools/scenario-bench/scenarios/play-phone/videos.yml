# MVP 默认模式：设备本地视频循环播放（Camera/AddVideo）。
# 注意：当前 ApplyParamsBatch 链路不带 videoRepeatCount，本地视频默认只读 1 轮，
# 读完任务会自动停止。因此每路 holdSec 不应显著超过单段视频时长。
# 长稳测试请改用 rtsp-deterministic 模式。详见设计稿第四节。
mode: local

local:
  - name: play-phone-01
    filePath: /opt/cosmo/video/play-phone.mp4
    fileName: play-phone.mp4
    contentLength: "20049063"
